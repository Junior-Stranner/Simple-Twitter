# Chave pública e privada JWT
jwt.public.key=classpath:app.pub
jwt.private.key=classpath:app.key

# Habilita a inicialização do SQL sempre que o aplicativo é iniciado
#spring.sql.init.mode=always
#spring.jpa.defer-datasource-initialization=true  # Comentado para desabilitar a inicialização tardia do datasource

# Configuração do Hibernate para atualizar o esquema do banco de dados automaticamente
#spring.jpa.hibernate.ddl-auto=update

# URL de conexão com o banco de dados PostgreSQL
#spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/mydb

# Nome de usuário do banco de dados
#spring.datasource.username=admin

# Senha do banco de dados
#spring.datasource.password=123

# PostgreSQL Database Connection Properties
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/twitter
spring.datasource.username=postgres
spring.datasource.password=123

# JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=update


# Driver JDBC para PostgreSQL
#spring.datasource.driver-class-name=org.postgresql.Driver

# Habilita a exibição de consultas SQL no console
#spring.jpa.show-sql=true

spring.flyway.baseline-on-migrate=true


# Springdoc OpenApi 3.1 & Swagger 3
springdoc.swagger-ui.path=/docs-twitter.html
springdoc.api-docs.path=/docs-twitter
springdoc.packagesToScan=com.jujubaprojects.simple-twitter.web.controller

admin.password=${ADMIN_PASSWORD}
jwt.secret.key=${JWT_SECRET}





